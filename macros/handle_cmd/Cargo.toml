[package]
name = "macro_handle_cmd"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
inventory = "0.3"

glue = { path = "../../glue" }
commands = { path = "../../commands" }