[package]
name = "glue"
version = "0.1.0"
edition = "2024"

[dependencies]
inventory = "0.3"
serenity = { version = "0.12", features = ["builder"], default-features = false }
songbird = "0.5"

utils = { path = "../utils" }